# GIA 도서관 시스템 — 설치·운영 가이드

학생 도서카드(바코드)로 대출·반납을 기록하는 시스템입니다. 운영앱(gia-ops)과 **같은 데이터베이스**를
쓰기 때문에 학생 명부를 두 번 관리할 필요가 없고, 나중에 출결·행사입장·매점으로 확장할 때도
**같은 카드**를 그대로 씁니다.

---

## 1. 지금 준비된 것

| 항목 | 위치 |
| --- | --- |
| 도서관 앱 소스 | `Desktop/GIA/Dev/gia-lib-web` |
| DB 표·보안규칙 | `gia-ops-web/supabase/migrations/20260821000000_library_system.sql` (schema.sql에도 반영) |
| 운영앱 보안 패치 | `gia-ops-web/src/lib/supabase/middleware.ts`, `src/app/login/page.tsx` |

운영앱은 손대지 않고 그대로 돌아갑니다. 추가된 것은 도서관 가계정을 운영앱에서 막는 규칙뿐입니다.

---

## 2. 준비물

| 물건 | 설명 | 대략 비용 |
| --- | --- | --- |
| USB 바코드 스캐너 | 1D(Code128 + EAN13) 지원이면 충분합니다. 유선 USB형이 가장 안정적입니다. | 2~4만원 |
| 도서관 노트북 | 브라우저만 있으면 됩니다. | 기존 것 |
| 카드용 두꺼운 종이 | 200g 이상 A4. 인쇄 후 잘라서 코팅합니다. | 1만원 내외 |
| 코팅기 + 코팅지 | 없으면 문구점 코팅 서비스도 가능합니다. | 3~5만원 |

> 스캐너는 **키보드처럼 입력하는 방식(HID)** 이 기본값입니다. 별도 드라이버나 설정 없이,
> 화면의 입력칸에 커서만 있으면 바로 동작합니다. 구입 후 메모장을 열고 아무 책 바코드를
> 찍어봤을 때 숫자가 찍히면 정상입니다.

---

## 3. 설치 순서

### 3-1. DB에 표 만들기

`gia-ops-web`을 GitHub main에 머지하면 GitHub Actions가 새 마이그레이션을 자동으로 실행합니다.
수동으로 하려면 Supabase 대시보드 → SQL Editor에 `20260821000000_library_system.sql` 내용을
붙여넣고 실행하세요.

만들어지는 것:

- `lib_books` — 장서(책) 목록
- `lib_loans` — 대출·반납 기록
- `lib_visits` — 도서관 입실 기록 (표만 미리 준비, 화면은 2단계)
- `lib_settings` — 대출 규칙 (2주 / 1인 3권 / 연장 +7일 1회 / 연체 시 대출 정지)
- `lib_students` — 도서관에서 볼 수 있는 학생 명부 뷰 (고유번호·이름·학년·반만)

### 3-2. 도서관 전용 가계정 만들기 (구글 계정 아님)

도서관 노트북은 **구글 로그인 대신 아이디+비밀번호**로 들어갑니다. 구글 워크스페이스에 계정을
새로 만들 필요가 없고, 우리 시스템(Supabase) 안에만 존재하는 가계정입니다.

1. Supabase 대시보드 → **Authentication** → **Users** → **Add user** → *Create new user*
2. Email `gia-library@giamicro.com` / 비밀번호는 직접 지정
3. **Auto Confirm User를 켜서** 만듭니다 (실제 메일함이 없으므로 확인 메일을 못 받습니다)
4. Authentication → **Providers** → Email 이 켜져 있는지 확인 (기본값은 켜짐)

이 계정은 마이그레이션이 실행될 때 운영앱의 계정 목록(`app_users`)에도 자동으로 등록됩니다.
따라서 **운영앱 계정 관리 화면에 "GIA 도서관(공용 단말)"로 나타나고, 승인을 취소하면 도서관
노트북도 즉시 막힙니다.** 이것이 통합 관리의 연결 고리입니다.

> - 비밀번호를 잊으면 메일이 없으므로 **Supabase 대시보드에서 직접 재설정**합니다.
> - 계정을 더 만들려면 `gia-library2@giamicro.com`처럼 `gia-library`로 시작하게만 지으면
>   같은 보안 규칙이 자동 적용됩니다(운영앱 접근 차단 + 도서관 데이터만 허용).
> - 선생님이 본인 구글 계정으로도 들어올 수 있습니다(로그인 화면의 "교직원 계정" 탭).

### 3-3. 앱 실행

```bash
cd ~/Desktop/GIA/Dev/gia-lib-web
npm install
cp .env.local.example .env.local
# .env.local 에 운영앱(gia-ops-web/.env.local)과 "똑같은" 두 값을 붙여넣습니다
#   NEXT_PUBLIC_SUPABASE_URL
#   NEXT_PUBLIC_SUPABASE_ANON_KEY
npm run dev        # http://localhost:3000
```

### 3-4. Vercel 배포 (실제 사용은 이걸로)

1. GitHub에 `gia-lib-web` 저장소를 새로 만들고 올립니다.
2. Vercel에서 Import → 환경변수 2개(`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)를 넣습니다.
3. 배포된 주소(예: `gia-library.vercel.app`)를 **Supabase → Authentication → URL Configuration →
   Redirect URLs**에 `https<!---->://…/auth/callback` 형태로 추가합니다.
4. 도서관 노트북 브라우저에서 그 주소를 열고 **도서관 계정** 탭에서
   `gia-library@giamicro.com` + 비밀번호로 한 번 로그인한 뒤, 즐겨찾기에 고정하거나 시작
   페이지로 설정합니다.

### 3-5. 노트북에 바탕화면 아이콘으로 설치하기

도서관 노트북에서는 주소창·탭이 없는 **전용 창**으로 띄우는 것이 좋습니다. 학생이 실수로
다른 사이트로 넘어가거나 탭을 닫을 일이 없어집니다.

- **크롬** — 배포된 주소를 연 뒤 주소창 오른쪽의 설치 아이콘(⊕), 또는 ⋮ 메뉴 →
  *캐스트, 저장 및 공유* → **페이지를 앱으로 설치**
- **엣지** — ⋯ 메뉴 → *앱* → **이 사이트를 앱으로 설치**
- **맥 사파리** — *파일* → **Dock에 추가**

설치하면 바탕화면(또는 독)에 GIA 아이콘이 생기고, 눌렀을 때 바로 대출·반납 화면이 뜹니다.
전체화면으로 쓰려면 창을 띄운 뒤 `F11`(맥은 `⌃⌘F`)을 누르세요.

### 3-6. (선택) ISBN 조회 품질 높이기

키를 하나도 안 넣어도 구글 북스로 대부분 조회됩니다. 한국 책 정보를 더 정확히 받고 싶으면
`.env.local`에 아래를 추가하세요.

- `ALADIN_TTB_KEY` — 알라딘 로그인 후 무료 발급. 한국 책 표지·정보가 가장 좋습니다.
- `NL_API_KEY` — 공공데이터포털 국립중앙도서관 ISBN 서지정보, 무료·즉시 승인.

---

## 4. 처음 한 번 하는 일

### 4-1. 도서카드 인쇄

관리 메뉴 → **도서카드 인쇄**

1. (선택) **배경 그림 올리기** — 학교에서 만든 카드 디자인을 올리면 그 위에 이름·바코드가
   얹혀 나갑니다. 가로로 긴 그림(권장 1012 × 638px 이상, 카드 비율 86:54)이 가장 잘 맞습니다.
   배경이 밝으면 글자색을 어둡게, 어두우면 흰색으로 바꾸세요.
2. (선택) **사진** — 학생 줄의 사진칸을 누르면 사진을 올릴 수 있고, **사진 넣어서 인쇄**를
   켜면 사진이 들어간 카드로 나갑니다. 끄면 이름만 들어갑니다(사진 없는 학생은 자동으로 이름만).
3. 부서/반으로 걸러서 **이 목록 전체 선택** → **선택한 N명 카드 인쇄**
4. 새 창에서 인쇄 → **배율 100%(실제 크기)**, **여백 없음**, **배경 그래픽 켜기**
5. A4 한 장에 10장이 나옵니다. 재단선을 따라 자르고 코팅하면 완성

카드에는 학생 고유번호(`GIA-2026-0001`)가 Code128 바코드로 들어갑니다. 학년이 바뀌어도 번호는
그대로이므로 **카드를 다시 만들 필요가 없습니다.** 바코드는 배경이 어떤 그림이든 **항상 흰
바탕 위에** 얹혀 인쇄되므로 스캔이 잘 됩니다.

### 4-2. 구역(책장 위치) 만들기

관리 메뉴 → **구역 관리**

1. 구역 이름을 자유롭게 추가합니다 — `A-1`처럼 책장·칸 번호도 되고 `그림책`처럼 분류 이름도 됩니다
2. 배치도에서 네모를 **끌어서** 실제 도서관 모양대로 놓습니다(크기는 ↔+ ↕+ 버튼으로 조절)
3. **책장 라벨 인쇄** → 90 × 55mm 라벨이 나옵니다. 책장 칸마다 붙이세요

라벨에는 큰 글씨(사람이 보는 용도)와 바코드(스캐너로 찍는 용도)가 함께 들어갑니다.

### 4-3. 장서 등록

**장서관리 → + 책 등록** 을 열고, 커서를 ISBN 칸에 둔 채 책 뒷면 바코드를 계속 찍습니다.

1. 바코드를 찍으면 제목·저자·출판사·표지가 자동으로 채워집니다
2. (필요하면) 서가 위치·분류를 입력하고 **등록하기**
3. 다음 책 반복 — 권당 5초 정도면 됩니다

ISBN이 없는 책은 **"ISBN 바코드가 없는 책입니다"** 를 켜고 제목만 입력하면 자체 번호
(`GIA-B-00001`)가 발급됩니다. 그런 책들만 모아 **라벨 인쇄** 로 뽑아서 붙이세요.

> **오래된 책·수입 원서 주의**
> - 표지에 **10자리 ISBN**(예: `0-441-01083-0`)만 적힌 책은 그대로 입력하시면 됩니다. 앱이
>   자동으로 13자리(`9780441010837`)로 바꿔 저장해서, 나중에 뒷면 바코드를 찍어도 같은 책으로
>   찾힙니다.
> - 미국 옛날 페이퍼백은 뒷면 바코드가 ISBN이 아니라 **상품코드(UPC, 12자리)** 인 경우가 많습니다
>   (`0 72742 00699 2` 처럼 생겼고, 오른쪽에 작은 바코드가 하나 더 붙어 있습니다). 이런 책은
>   찍으면 "표지의 ISBN을 입력해 주세요"라고 안내가 뜹니다. ISBN을 입력해 등록하면 **찍었던 UPC도
>   함께 저장**되어, 다음부터는 그 바코드로도 대출·반납이 됩니다.

> 500권이면 대략 1시간 안쪽입니다. 학생 도우미 두세 명이 나눠 하면 더 빠릅니다.

**ISBN 숫자는 있는데 바코드가 안 찍혀 있는 책**(오래된 책·수입 원서 등)은 ISBN을 손으로
입력해 등록한 뒤, 장서관리에서 그 책을 골라 **바코드 라벨 인쇄**를 누르면 됩니다. 그 ISBN으로
바코드를 만들어 주므로, 붙인 뒤에는 다른 책과 똑같이 찍으면 됩니다.

**새로 구입한 책**도 같은 방법입니다 — 책 등록에서 뒷면 바코드를 찍으면 3초면 끝나고,
바코드가 없으면 ISBN을 입력하거나 자체 번호를 발급받아 라벨을 붙입니다.

### 4-4. 휴대폰으로 책 등록하기 (권장)

책장 앞에서 노트북 없이 등록할 수 있습니다.

1. 노트북의 **장서관리 → 📱 휴대폰으로 등록**을 누르면 QR이 뜹니다. 휴대폰 카메라로 찍으면
   모바일 등록 화면이 열립니다(휴대폰에서 한 번만 로그인하면 그다음부터는 바로 열립니다).
2. **바코드 찍기** — 책 뒷면 ISBN을 폰 카메라로 읽습니다. 잘 안 읽히면 숫자를 직접 입력해도 됩니다.
3. **정보 확인** — 제목·지은이·출판사가 자동으로 채워집니다. 구역과 권수를 정합니다.
4. **표지 찍기** — 폰 카메라로 표지를 찍고 파란 네모를 맞추면 **표지 부분만 잘라서** 함께
   등록됩니다. 인터넷에서 표지를 찾은 책은 이 단계를 건너뛸 수 있습니다.
5. 등록이 끝나면 **다음 책 등록**을 눌러 바로 이어서 진행합니다.

> - 표지가 없던 기존 책도 바코드를 찍으면 &lsquo;표지만 추가&rsquo;로 넘어가 채워 넣을 수 있습니다.
> - 아이폰 사파리는 브라우저에 바코드 인식 기능이 없어서 인식기를 따로 넣어 두었습니다. 처음
>   카메라를 켤 때 **카메라 권한 허용**을 눌러주세요(설정 → Safari → 카메라).
> - 표지 사진은 가로 900px JPG로 줄여 저장하므로 데이터 사용량이 크지 않습니다.

### 4-5. 책을 책장에 꽂고 구역 배정하기

관리 메뉴 → **책 정리** → **구역 배정** 탭

1. 책장 칸에 붙인 **구역 라벨을 한 번 찍습니다**
2. 그 칸에 꽂을 **책들을 주르륵 찍습니다** — 전부 그 구역으로 배정됩니다
3. 다른 칸으로 옮길 때는 그 칸 라벨을 다시 찍고 이어서 진행합니다

한 칸 정리에 1분이면 충분하고, 나중에 책 위치가 바뀌어도 다시 찍기만 하면 갱신됩니다.

---

## 5. 화면 구성 (도서관 전용 단말 기준)

데스크에 놓고 학생과 함께 보는 화면이라 이렇게 만들었습니다.

- **위쪽 남색 띠** — GIA 로고와 도서관 이름, 오른쪽 끝에 `☰ 관리` 버튼
- **가운데 큰 색 상자** — 방금 처리한 결과. 초록=대출, 파랑=반납, 주황=주의, 빨강=오류
- **아래 흰 카드** — 지금 카드를 찍은 학생의 이름(아주 크게)과 빌린 책·반납예정일.
  건너편에 앉은 학생도 자기 반납일을 읽을 수 있는 크기입니다
- **다른 학생의 이름·빌린 책은 화면에 뜨지 않습니다.** 옆 사람이 볼 수 있는 자리라
  오늘 처리 건수(숫자)만 위에 작게 표시합니다. 전체 기록은 관리 메뉴에서 봅니다
- **관리 메뉴는 평소 숨어 있습니다.** `☰ 관리`를 누르면 오른쪽에서 서랍처럼 나오고,
  장서관리·대출현황·도서카드 인쇄·설정이 들어 있습니다. 학생이 실수로 장서를 고칠 일이 없습니다
- 학생 표시는 25초 뒤 자동으로 사라집니다. 바로 지우려면 `Esc`

## 6. 매일 쓰는 법

**대출·반납** 화면 하나만 띄워두면 됩니다. 마우스를 만질 필요가 없습니다.

| 상황 | 하는 일 |
| --- | --- |
| 빌려줄 때 | 학생카드 → 책 (순서대로 찍기) |
| 여러 권 빌려줄 때 | 학생카드 → 책 → 책 → 책 |
| 반납받을 때 | 책만 찍기 |
| 처음 보는 책이 왔을 때 | 찍으면 "등록되지 않은 책" → **이 책 등록하기** 버튼 |
| 카드를 안 가져온 학생 | 이름을 입력하고 Enter — 동명이인이면 골라줍니다 |
| 반납받은 책을 꽂을 때 | 화면에 뜬 📍 구역으로 가져갑니다 |
| 책이 어디 있는지 물을 때 | 위쪽 **🔎 책 찾기** — 학생이 직접 써도 됩니다 |
| 학생 잘못 찍었을 때 | `Esc` 키 |

- 성공하면 초록색 + 높은 신호음, 문제가 있으면 빨간색 + 낮은 신호음이 납니다.
- 학생을 찍으면 그 학생이 지금 빌린 책과 연체 여부가 바로 보입니다.
- 학생 표시는 25초 뒤 자동으로 사라집니다(다음 학생과 섞이지 않게).

### 규칙 위반 시 자동 차단

- 1인 3권을 채운 학생 → "최대 3권을 채웠습니다"
- 연체 중인 학생 → "연체 중인 책이 있어 대출할 수 없습니다"
- 보유 권수가 다 나간 책 → "보유한 권수가 모두 대출중입니다"

규칙은 **설정** 화면에서 언제든 바꿀 수 있습니다.

---

## 7. 독서 도감 (분류)

도서카드를 찍으면 학생 화면에 **독서 도감**이 나옵니다. 분류별로 몇 권 읽었는지 칸이 채워지고,
아직 안 읽은 칸은 흐리게 보입니다. 편식 독서를 스스로 알아채고 빈 칸을 채우고 싶어지도록 만든
장치입니다.

**분류는 등록할 때 자동으로 붙습니다.** ISBN을 조회하면 알라딘의 카테고리, 국립중앙도서관의
KDC 번호, 구글 북스의 categories를 읽어서 우리 칸으로 옮깁니다. 자동 분류가 틀리면 등록 화면의
드롭다운에서 바로 고칠 수 있고, 나중에 장서관리에서 분류별로 걸러 보며 정리할 수도 있습니다.

지금 칸은 열 개입니다 — 그림책 / 동화·소설 / 시·희곡 / 역사·인물 / 과학·자연 / 사회·문화 /
예술·체육 / 만화·잡지 / 학습·참고 / 기타. **장서를 어느 정도 등록한 뒤 실제 책 구성을 보고
칸을 다시 정하는 게 좋습니다.** 칸 이름과 자동 분류 규칙은 `src/lib/categories.ts` 한 파일에
모여 있습니다.

## 8. 도서정리 — 한 번에 제자리 찾기

지금처럼 **책을 아무 데나 꽂아둔 상태에서 시작해도 됩니다.** 순서는 이렇습니다.

### 8-1. 지금 꽂힌 대로 임시구역 만들기

책장 사진을 보고 지금 꽂혀 있는 칸마다 구역을 하나씩 만듭니다. 관리 메뉴 → **구역 관리**에서
이름을 `임시-1`, `임시-2`… 로 짓고 종류를 **임시**로 고릅니다. 임시구역은 "지금 있는 자리"일
뿐이라 정리 목적지가 되지 않습니다.

### 8-2. 지금 붙어 있는 색 라벨은 그대로 적어둡니다

책에 이미 붙어 있는 색 라벨(숫자 2~6 + 일련번호)은 **버리지 말고 있는 그대로 기록**합니다.
새 체계로 바꾸더라도 원래 번호가 남아 있어야 "등록이 빠진 책이 있는지"를 번호로 확인할 수 있고,
예전 방식으로 찾는 사람에게도 대응할 수 있기 때문입니다.

번호를 손으로 치는 게 부담이라 생각하실 텐데, **거의 자동입니다.** 여러 권 등록 화면 위쪽에
등급(예: `4등급`)과 **시작 번호**(예: `001`)를 한 번만 넣으면, 바코드를 찍을 때마다 번호가
002, 003… 으로 하나씩 올라갑니다. 책이 번호순으로 꽂혀 있으면 손댈 일이 없고, 번호가 건너뛰면
그 줄에서만 고치면 됩니다. `007`처럼 앞자리 0을 넣으면 자릿수를 지켜서 올라갑니다.

**001부터 시작하는지, 빠진 번호가 있는지는 등록해 보면 알 수 있습니다.** 관리 메뉴 →
**지금 라벨 점검**에서 등급마다 번호 범위와 빠진 번호 목록이 나옵니다. 빠진 번호는 잃어버린
책이거나 아직 등록 안 한 책입니다. 이 화면에서 등급마다 실제 라벨색과 이름(예: `4 = 저학년`)도
지정할 수 있고, 나중에 "4등급 = 초등부"처럼 대상 연령과 짝지어 두면 정리 계획이 그 짝을
그대로 씁니다.

라벨 등급을 기준으로 정리하고 싶으면 계획 화면에서 **지금 라벨 등급 → 분류 → 작가**를 고르면
됩니다. 기존 등급 체계를 유지하면서 그 안만 정리하는 방식입니다.

### 8-3. 한 칸씩 통째로 등록하기

관리 메뉴 → **여러 권 등록**에서 구역을 `임시-1`로 고르고, 그 칸 책을 쭉 빼서 바코드를 연속으로
찍습니다. 제목·지은이·표지·분류·대상 연령이 인터넷에서 자동으로 채워집니다. 다 찍었으면
한 번만 눌러 통째로 등록하고, 책은 원래 칸에 도로 꽂습니다. 다음 칸도 같은 방법으로.

> 그 칸 책들이 대체로 한 연령대면 위쪽 **대상** 칸에서 미리 골라두세요. 자동 추정이 비어 있는
> 책에만 그 값이 채워집니다.

**같은 책이 여러 권이면 그냥 다 찍으세요.** 이미 등록된 책을 또 찍으면 새로 만들지 않고
**보유 권수를 한 권 올립니다**(목록에 `복본`으로 표시됩니다). 도서관에서 같은 책 두 권은
'다른 책'이 아니라 '한 책 두 권'이라, 따로 등록하면 검색 결과에 같은 제목이 두 줄 뜨고 독서
도감 통계도 두 번 세어집니다.

**시리즈는 자동으로 묶입니다.** `마법천자문 12권`, `Harry Potter #3` 처럼 제목에 권 번호가
있으면 시리즈 이름과 권수를 알아서 떼어냅니다(알라딘 조회가 되면 정확한 시리즈 이름이 옵니다).
정리할 때 **시리즈는 같은 칸에 1권부터 차례로 붙여서** 놓고, 시리즈에 속하지 않는 낱권이 그
뒤에 작가순으로 놓입니다. 아이가 다음 권을 바로 옆에서 집을 수 있어야 시리즈 독서가 이어지기
때문입니다. 자동으로 뽑은 시리즈 이름이 틀리면 등록 화면이나 장서관리에서 고치면 됩니다.

### 8-4. 칸 용량 기록하기

한 칸을 다 등록했으면 **구역 관리**로 가서 그 칸의 **← N권으로** 버튼을 누릅니다. 방금 그 칸에서
나온 권수가 곧 그 칸에 실제로 들어가는 양이라, 이 숫자가 정리할 때 "넘치지 않게 나눠 담는"
기준이 됩니다.

### 8-5. 정리 후에 쓸 칸(정식 구역) 만들기

정리를 마친 뒤 쓸 칸들을 **정식**으로 만듭니다. 임시구역과 물리적으로 같은 칸이어도 상관없습니다
— 이름만 실제 쓸 이름(`A-1`, `B-2`…)으로 지으면 됩니다. 용량도 적어둡니다.

### 8-6. 계획 세우기

관리 메뉴 → **도서정리 계획**. 기준을 고르면(기본값 **대상 연령 → 분류 → 작가**) 곧바로
미리보기가 나옵니다.

- **옮겨야 할 책 / 그대로 둘 책 / 쓰는 칸 / 자리 못 받은 책** 네 숫자로 규모가 보입니다
- 칸마다 무엇이 몇 권 들어가는지, 용량을 넘치는 칸은 없는지 막대로 보입니다
- 대상 연령이나 분류가 비어 있는 책이 몇 권인지 알려줍니다 — 많으면 먼저 장서관리에서 채우는
  편이 결과가 훨씬 깔끔합니다

마음에 들면 **이 계획으로 확정**. 확정해도 **책이 지금 꽂혀 있는 자리는 바뀌지 않습니다.**
"가야 할 자리"만 적어둘 뿐이라, 정리하는 며칠 동안에도 학생이 찾는 책의 위치를 정확히 답할 수
있습니다.

### 8-7. 실제로 옮기기 — 두 가지 방법

**① 종이 목록** (계획 화면 → 🖨 이동 목록 인쇄)

`지금 자리: 임시-1` 처럼 **지금 꽂혀 있는 칸별로 묶여** 나옵니다. 책장 앞에 서서 그 칸을 통째로
빼면서 종이를 위에서 아래로 훑으면 됩니다. 각 줄 오른쪽에 **→ B-2** 처럼 갈 칸이 크게 적혀 있고,
체크 상자도 있습니다. 인터넷이 없어도 되고, 여러 명이 종이를 나눠 들고 할 수 있습니다.

**② 화면에서 찍기** (관리 메뉴 → 정리 실행)

책 바코드를 찍으면 화면 가득 **B-2** 처럼 갈 칸이 뜹니다. 세 걸음 떨어진 책장 앞에서도 읽힙니다.
찍는 순간 "옮겼다"로 기록되므로 남은 권수가 실시간으로 줄어듭니다. 잘못 찍었으면 아래 목록에서
**되돌리기**를 누르면 됩니다. 이미 제자리인 책은 초록색으로 **그대로 두세요**가 뜹니다.

두 방법을 섞어 써도 됩니다. 종이로 뽑아 옮기고 마지막에 화면으로 한 번 훑어 확인하는 식으로요.

### 8-8. 정리가 끝나면

임시구역은 비게 됩니다. **구역 관리**에서 지우거나, 다음에 새 책이 들어왔을 때 다시 임시 칸으로
쓰면 됩니다. 정식 칸에는 "초등부 · 과학·자연" 같은 배정 내용이 적혀 있어서, 이후 반납된 책도
제자리로 돌아갑니다.

## 9. 책 찾기와 반납 정리

**책 찾기** (대출 화면 위쪽 🔎 버튼 또는 관리 메뉴)

제목 일부만 쳐도 결과가 바로 뜹니다. 책을 누르면 📍 구역이 큰 글씨로 뜨고, 배치도에서 그
자리가 반짝입니다. 지금 대출중인지(몇 권 남았는지)도 함께 보입니다. 학생이 직접 써도 됩니다.

**반납 정리** (관리 메뉴 → 책 정리 → 반납 정리)

반납받아 아직 안 꽂은 책이 **구역별로 묶여** 나옵니다. 반납함(책 넣는 통)에 들어온 책도 스캔해
반납 처리하면 여기에 나타납니다. 책수레에 모아뒀다가 구역 순서대로 한 바퀴 돌면 정리가 끝납니다.
꽂은 책은 **꽂았음**을 누르거나, 구역 배정 탭에서 찍으면 목록에서 사라집니다.

## 10. 다음 단계 (확장)

### 2단계 — 도서관 입실 기록

표(`lib_visits`)와 권한은 이미 만들어져 있고 화면만 붙이면 됩니다. 탁상형 스캐너를 입구에
하나 더 두면 "책을 안 빌려도 도서관을 이용한 학생"이 집계되어, 실제 이용률·예산 근거 데이터가
쌓입니다.

### 3단계 — 학생증 통합

지금 구조(카드 찍기 → 누가/언제/무엇을 기록)는 그대로 두고 대상만 바꾸면 됩니다.

- **출결** — 등교 시 카드 태그 → `attendance_records`에 자동 기록 (운영앱에 이미 있는 표)
- **행사 입장** — 행사별 입장 명단·인원 자동 집계
- **물품 구입** — 매점·교재 판매 기록, 학기말 정산

카드는 이미 발급되어 있으므로 추가 비용은 스캐너뿐입니다.

---

## 11. 문제가 생기면

| 증상 | 확인할 것 |
| --- | --- |
| 바코드를 찍어도 아무 반응이 없음 | 화면의 입력칸에 커서가 있는지. 화면 아무 곳이나 클릭하면 자동으로 커서가 돌아옵니다 |
| 학생카드가 "등록되지 않은 학생카드" | 운영앱 학생 명부에 그 학생이 있는지, 고유번호가 맞는지 |
| 휴대폰에서 카메라가 안 켜짐 | 설정 → Safari(또는 크롬) → 카메라 권한 허용. 주소가 https인지도 확인 |
| 휴대폰에서 바코드가 안 읽힘 | 밝은 곳에서 20~30cm 거리로. 그래도 안 되면 ISBN 숫자를 직접 입력 |
| 책 정보가 자동으로 안 채워짐 | 인터넷 연결. 오래된 책·자체 제작물은 정보가 없을 수 있으니 제목만 직접 입력해도 됩니다 |
| 카드 크기가 안 맞음 | 인쇄 배율을 100%(실제 크기)로. "페이지에 맞춤"이 켜져 있으면 작게 나옵니다 |
| 도서관 계정으로 운영앱이 안 열림 | 정상입니다. 보안을 위해 의도적으로 막아둔 것입니다 |
| 도서관 계정 로그인 시 "승인 상태가 아닙니다" | 운영앱 계정 관리에서 "GIA 도서관(공용 단말)"을 승인으로 되돌리면 됩니다 |
| 도서관 계정 비밀번호를 잊음 | Supabase 대시보드 → Authentication → Users에서 재설정 (메일함이 없어 메일 재설정은 안 됩니다) |
| 배경 그림이 인쇄에 안 나옴 | 브라우저 인쇄 설정에서 **배경 그래픽**을 켜주세요 |
| 배경 그림 업로드가 안 됨 | Supabase 대시보드 → Storage에 `library` 버킷이 있는지 확인(공개로 설정) |
| 책을 찍었는데 구역이 안 뜸 | 그 책은 아직 구역 배정 전입니다. 책 정리 → 구역 배정에서 자리를 정해주세요 |
