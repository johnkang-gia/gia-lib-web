import pkg from "../../package.json";

/**
 * 화면에 뜨는 현재 버전.
 *
 * package.json 한 곳에서만 읽습니다. 같은 숫자를 두 곳에 적어두고 사람이 맞추기로 하면
 * 언젠가 반드시 어긋나고, 그때부터 화면의 버전은 거짓말이 됩니다. 운영앱에서 실제로 그런
 * 일이 있었습니다.
 *
 * `import`는 빌드할 때 값이 코드 안으로 박히므로, 배포된 화면에 뜨는 숫자는 **그 화면을 만든
 * 코드의 버전**입니다. 새로고침했는데 숫자가 그대로면 아직 배포가 안 끝난 것입니다.
 */
export const APP_VERSION: string = pkg.version;
